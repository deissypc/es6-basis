//importar commonjs
import {sumar, restar,multiplicar,dividir,potencia} from './operaciones.js'

const a =10;
const b=30;
console.log(`la suma es:${sumar(a,b)}`)
console.log(`la restar es:${restar(a,b)}`)
console.log(`la multiplicar es:${multiplicar(a,b)}`)
console.log(`la dividir es:${dividir(a,b)}`)
console.log(`la potencia es:${potencia(a,b)}`)